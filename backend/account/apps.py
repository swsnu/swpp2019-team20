"""Django App
This is the Django application config file for Account
"""

from django.apps import AppConfig

class AccountConfig(AppConfig):
    """Account Config
    Define the name of the app
    """
    name = 'account'
