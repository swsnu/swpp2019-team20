import React from 'react';
import AppForm from '../../components/subcomponents/AppForm';

const LoanList = () => {
  // mock DB
  const render = (
    <AppForm>
      <h3>LoanList</h3>
    </AppForm>
  );
  return render;
};

export default LoanList;